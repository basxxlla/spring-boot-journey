<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>

        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/demo_db?useSSL=false&amp;serverTimezone=UTC&amp;createDatabaseIfNotExist=true</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">root</property>

        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.hbm2ddl.auto">update</property>
        <property name="hibernate.connection.pool_size">5</property>

        <!-- Part 1: Player CRUD demo -->
        <mapping class="com.example.player.Player"/>

        <!-- Part 2: Hospital / Doctor / DoctorDetails / Patient relationship demo -->
        <mapping class="com.example.hospital.Hospital"/>
        <mapping class="com.example.hospital.Doctor"/>
        <mapping class="com.example.hospital.DoctorDetails"/>
        <mapping class="com.example.hospital.Patient"/>

        <!--
          Part 3: the 20 reference relationship pairs under com.example.relationships.*
          are NOT mapped here on purpose - they are a reference "cookbook" of entities,
          not a single coherent runnable schema (e.g. several reuse simple names like
          "Order" or "Room"). To try one out, add a <mapping class="..."/> line for
          its two entities and point hibernate.connection.url at a suitable database.
        -->

    </session-factory>
</hibernate-configuration>
